'use strict';

/**
 * @ngdoc function
 * @name djangoBulstradApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the djangoBulstradApp
 */
angular.module('djangoBulstradApp')
  .controller('MainCtrl', ['$scope', 'Constants', function ($scope, Constants) {
  }]);
